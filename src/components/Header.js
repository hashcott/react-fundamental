import React from "react";
import Form from "./Form";

// react-hooks
const Header = () => {
  return (
    <header>
      <h1>Todo list</h1>
      <Form />
    </header>
  );
};

export default Header;
